# Supabase 接続情報(Phase 4 以降で使用)
# .env.local にコピーして値を設定してください(.env* は git 管理外)
# anon key は公開前提のキーです(保護は Supabase 側の RLS で行う)。
# service_role key は絶対にここに書かないでください。
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
