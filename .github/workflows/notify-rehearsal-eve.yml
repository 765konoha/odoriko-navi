name: Notify rehearsal eve

# リハの前日21時(JST)に「明日はリハです」を送る。
# 21:00 JST = 12:00 UTC。cron は UTC で書く。
#
# Supabase のダッシュボードで pg_cron を仕掛ける代わりに、
# 既に使っている GitHub Actions から Edge Function を叩く。
#
# 必要な Secrets:
#   VITE_SUPABASE_URL       … デプロイでも使っているもの
#   VITE_SUPABASE_ANON_KEY  … 同上(Edge Function の入口を通るため)
#   CRON_SECRET             … 新規。関数側の Secrets と同じ文字列にする
#                             (service_role キーを置かずに済ませるため)
on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:
    inputs:
      dryRun:
        description: '送らずに対象件数だけ確認する'
        type: boolean
        default: false
      force:
        description: '夜以外(日本時間の正午より前)でも送る'
        type: boolean
        default: false

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Call notify-rehearsal-eve
        env:
          URL: ${{ secrets.VITE_SUPABASE_URL }}
          ANON: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          DRY_RUN: ${{ inputs.dryRun }}
          FORCE: ${{ inputs.force }}
        run: |
          if [ -z "$URL" ] || [ -z "$ANON" ] || [ -z "$CRON_SECRET" ]; then
            echo "::error::VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY / CRON_SECRET を設定してください"
            exit 1
          fi
          body='{}'
          if [ "$DRY_RUN" = "true" ]; then body='{"dryRun":true}'
          elif [ "$FORCE" = "true" ]; then body='{"force":true}'; fi

          # 応答本文と状態コードを分けて受け取る
          out=$(mktemp)
          code=$(curl -sS -o "$out" -w '%{http_code}' \
            -X POST "$URL/functions/v1/notify-rehearsal-eve" \
            -H "Authorization: Bearer $ANON" \
            -H "x-cron-secret: $CRON_SECRET" \
            -H 'Content-Type: application/json' \
            -d "$body")
          echo "HTTP $code"
          cat "$out"
          echo
          if [ "$code" != "200" ]; then
            echo "::error::通知の送信に失敗しました (HTTP $code)"
            exit 1
          fi
